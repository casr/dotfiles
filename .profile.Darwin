PATH=$HOME/bin:$PATH
command -v npm >/dev/null && PATH=$(npm -g bin 2>/dev/null):${PATH}
export PATH

EDITOR=vim
FCEDIT=$EDITOR
export EDITOR FCEDIT

HISTCONTROL=ignoredups:ignorespace
HISTSIZE=5000
SAVEHIST=5000
HISTFILE=$HOME/.sh_history
export HISTCONTROL HISTFILE HISTSIZE SAVEHIST
